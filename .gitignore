# ── Dependencias ─────────────────────────────────────────────
node_modules/

# ── Builds y artefactos generados ────────────────────────────
dist/
build/
# Solo la carpeta de cobertura de tests en la raíz — NO .ai/coverage/ (docs canónicas)
/coverage/
*.tsbuildinfo

# ── Temporales de sincronizadores de nube (Google Drive/OneDrive) ──
.tmp.driveupload/
.tmp.drivedownload/
.tmp-*
.tmp/
temp/

# ── Logs ─────────────────────────────────────────────────────
*.log
logs/
npm-debug.log*

# ── Entornos y secretos ──────────────────────────────────────
.env
.env.*
!.env.example

# ── Salidas de testing ───────────────────────────────────────
playwright-report/
test-results/

# ── Caches de herramientas ───────────────────────────────────
.eslintcache
.vite/
.turbo/
.npm/

# ── Artefactos de sistema operativo ──────────────────────────
.DS_Store
Thumbs.db
Desktop.ini

# ── IDEs ─────────────────────────────────────────────────────
.vscode/
.idea/
*.swp

# ── Documentos de trabajo por-sprint ──────────────────────────
# walkthrough.md: bitácora global/efímera — propuesta pendiente de aprobación
# definitiva (ver docs/designs/document-architecture-cleanup.md §3). Se
# mantiene ignorado en esta fase, sin cambios.
walkthrough.md
# implementation_plan.md: NO se ignora. Corrección de gobernanza (Sprint 040,
# Lote A) — los planes de implementación son documentación versionada por
# política oficial (docs/designs/document-architecture-cleanup.md §1). La
# línea que lo ignoraba era un error, no una decisión aprobada.
